version: '3.8'

services:
  credit-checker-even:
    build: .
    container_name: credit-checker-even
    environment:
      - DISPLAY=:99
      - PYTHONUNBUFFERED=1
      - BATCH_SIZE=5  # Number of claims to process per cycle
      - FILTER_TYPE=even
    volumes:
      # Mount the output directory to persist results
      - ./output:/app/output
      - ./logs:/app/logs
      # Mount the overall_checked_claims.csv to persist processed claims
      - ./overall_checked_claims.csv:/app/overall_checked_claims.csv
      # Mount claims.csv to persist results
      - ./claims.csv:/app/claims.csv
    # Restart policy - always restart on failure
    restart: unless-stopped
    # Resource limits (optional)
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G
    # Security options for Chrome
    security_opt:
      - seccomp:unconfined
    # Shared memory for Chrome
    shm_size: 2gb
    # Health check to monitor the container
    healthcheck:
      test: ["CMD", "pgrep", "-f", "python3 scraping.py"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
